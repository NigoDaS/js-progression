let tasks = [];


// task :       {
//   id: 1,
//     title: "Buy groceries",
//       category: "personal",
//         priority: "high",
//           completed: false
// }



function addTask(title, category, priority) {
  //creates task object with id, adds to array, returns task
}

function toggleTaskComplete(taskId) {
  //finds task by id, toggles completed property
}

function getTasksByCategory(category) {
  // returns filtered array of tasks matching category
}

function getCompletedTasks() {
  //returns filtered array of completed tasks
}

function getStatistics() {
  //returns object with: totalTasks, completedTasks, completionRate (percentage), tasksByCategory (object with counts per category)
}